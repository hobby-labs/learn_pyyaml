# Command

```bash
docker run --volume ${PWD}:/opt/learn_pyyaml -ti python:3 bash

cd /opt/learn_pyyaml
python -m venv venv/
source venv/bin/activate

pip install --upgrade pip
pip install -r requirements.txt
```
